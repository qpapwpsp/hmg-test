<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hmg.as.test.hmg_test.mapper.EmployeeMapper">
        <!-- 직원 조회 쿼리 -->
    <select id="findById" parameterType="Long" resultType="String">
        SELECT FIRST_NAME
          FROM EMPLOYEES
         WHERE employee_id = #{employeeId}
    </select>
    
    <update id="updateJobtTtle" parameterType="Long">
        UPDATE EMPLOYEES
           SET phone_number = '010-9999-9999'
         WHERE EMPLOYEE_ID = #{employeeId}
    </update>
</mapper>